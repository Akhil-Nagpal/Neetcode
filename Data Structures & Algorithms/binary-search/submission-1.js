class Solution {
    /**
     * @param {number[]} nums
     * @param {number} target
     * @return {number}
     */
    search(nums, target) {
        // Optimised Code using Binary Search

        // start from both ends left and right
        let left = 0;
        let right = nums.length - 1;
        
        while(left <= right) {
            
        }
    }
}
